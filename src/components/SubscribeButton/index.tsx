import styles from "./style.module.sass";

export const SubscribeButton = () => {
    return (
        <button
            type="button"
            className={styles.subscribeButton}
        >
            Subscribe Now
        </button>
    )
}
